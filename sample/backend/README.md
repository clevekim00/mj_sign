# Backend samples

- `model_server/`: SignGemma 호환 FastAPI mock/실험 서버
- `web_app/`: React/Tauri 데모
- `web/`: 최소 WebSocket 웹 예제
- `docker-compose*.yml`: HTTP, Kafka, RabbitMQ 통합 스택

HTTP 통합 검증:

```sh
./scripts/verify_docker_http_stack.sh
```

