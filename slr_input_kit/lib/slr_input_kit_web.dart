import 'package:flutter_web_plugins/flutter_web_plugins.dart';

class SlrInputKitWeb {
  static void registerWith(Registrar registrar) {
    // The package API is currently pure Dart. This no-op registrar declares
    // web support so Flutter apps can use the WebSocket sample without native FFI.
  }
}
