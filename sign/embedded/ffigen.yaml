# Run with `dart run ffigen --config ffigen.yaml`.
name: SlrInputKitBindings
description: |
  Bindings for `src/slr_input_kit.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/slr_input_kit_bindings_generated.dart'
headers:
  entry-points:
    - 'src/slr_input_kit.h'
  include-directives:
    - 'src/slr_input_kit.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
